import * as THREE from 'three';
window.makeLabel = function(text){const c=document.createElement('canvas'),x=c.getContext('2d');x.font='600 40px ui-monospace,monospace';c.width=x.measureText(text).width+24;c.height=60;x.fillStyle='#d9efeb';x.fillText(text,12,42);const s=new THREE.Sprite(new THREE.SpriteMaterial({map:new THREE.CanvasTexture(c),transparent:true,depthTest:false}));s.scale.set(c.width/64*.35,.35,1);return s;};
